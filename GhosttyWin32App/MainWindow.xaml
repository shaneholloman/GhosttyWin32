<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GhosttyWin32.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GhosttyWin32"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Ghostty">

    <Grid>
        <TabView x:Name="TabView"
                 IsAddTabButtonVisible="True"
                 TabWidthMode="Equal"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Stretch">
            <TabView.TabStripFooter>
                <!-- Stretch fills the remaining strip width after tabs + [+],
                     so the drag region naturally shrinks as more tabs are
                     opened and grows back when tabs are closed. The MinWidth
                     stops it from collapsing under the system caption buttons
                     (~138px) — 200px keeps ~62px of usable drag area visible
                     to the left of the buttons even when the strip is full. -->
                <Border x:Name="DragRegion" Background="Transparent"
                        HorizontalAlignment="Stretch" MinWidth="200" />
            </TabView.TabStripFooter>
        </TabView>
    </Grid>
</Window>
